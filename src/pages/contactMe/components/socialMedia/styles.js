import styled from 'styled-components';

const SocialMediaCSS = styled.div`
    display: flex;
    justify-content: center;
    .icons {
        width: 4rem;
        margin: 2rem 1.5rem;
    }
    .icons:hover {
        filter: brightness(0.50);
        
    }
`

export default SocialMediaCSS;